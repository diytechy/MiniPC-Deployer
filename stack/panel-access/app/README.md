Place the verified OfficeWallNaglight gateway artifact's `gateway/` directory
here. Application source belongs to OfficeWallNaglight and is not copied into
this repository. Never stage gateway state, PINs or credentials here.
