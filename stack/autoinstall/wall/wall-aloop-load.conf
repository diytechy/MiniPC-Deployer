# Load the loopback the trigger detector reads the kiosk's audio through.
# Options (index, substreams) are in modprobe.d/wall-aloop.conf; this is what
# actually causes the module to be loaded at boot.
snd-aloop
